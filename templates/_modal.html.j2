{% from "_macros.html.j2" import form_block %}
<div class="mdl" data-mdl role="dialog" aria-modal="true">
  <div class="mdl__bg" data-mdl-x></div>
  <div class="mdl__b">
    <button class="mdl__x" type="button" data-mdl-x aria-label="Закрыть">×</button>
    {{ form_block(
        'm',
        'Вызвать замерщика',
        'Оставьте телефон. Менеджер перезвонит в рабочее время, уточнит объект и договорится об удобном времени.',
        'Вызвать замерщика',
        'Модальное окно'
    ) }}
  </div>
</div>
